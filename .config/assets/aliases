# Aliases

## If bat is installed, 'use' it instead of cat
if [[ $(which bat) ]] 2>/dev/null; then
    alias cat='bat'
fi

alias ll='ls -l'
alias grep='grep --color'
alias less='less -rf'
alias mv='mv -i'
alias rm='rm -i'
alias cp='cp -i'
