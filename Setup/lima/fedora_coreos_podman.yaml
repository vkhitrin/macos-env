---
# This example requires Lima v0.8.0 or later

arch: "default"
images:
  - location: "/tmp/fedora-coreos-36.20221030.3.0-live.aarch64.iso"
    arch: "x86_64"
cpus: 2
memory: "2GiB"
disk: "60GiB"
mounts:
  - location: "~"
    writable: false
  - location: "/tmp/lima"
    writable: true
containerd:
  system: false
  user: false
ssh:
  localPort: 60906
# Did not get this to work properly, socket is created but can not communicate with it
# portForwards:
  # - guestSocket: "/run/podman/podman.sock"
    # hostSocket: "{{.Dir}}/sock/podman.sock"
firmware:
  legacyBIOS: true
video:
  display: "none"
message: |
  To run `podman` on the host:
  podman system connection add -p 60906 --identity ~/.ssh/podman podman root@127.0.0.1/run/podman/podman.sock
